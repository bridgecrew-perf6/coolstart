{% extends "layouts/base.njk" %}
{% set pageStylesheets = ['css/styles.css'] %}


{% block content %}
    <section>
        <h1>{{title}}</h1>
        <img src="{{hero.image}}" alt="{{hero.alt}}">
    </section>
{% endblock %}